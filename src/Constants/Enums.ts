export enum fuelType {
  GASOLINE = 'Gasolina',
  DIESEL = 'Diesel',
  ETHANOL = 'Etanol',
  FLEX = 'Flex',
}

export enum orderStatus {
  PENDING = '1',
  EXECUTING = '2',
  EXECUTED = '3',
  CANCELED = '4',
  DELETED = '5',
}

export enum ScreenStatus {
  VISUALIZE = 'visualize',
  NEW = 'new',
  EDIT = 'edit',
}

export enum PersonType {
  PHYSICAL = '1',
  LEGAL  = '2',
}
